* This Section helps us to understand different file and directory commands. 
* Linux shell is a program that allows the interaction between the user and operating system. 
* Navigate different locations within the system. 
* Default directory is HOME Directory. 
* Home directory allows user to store the personal data. 

# **ECHO Command**

$ echo Hello

Hello

$ echo -n Hello

Hello [~] $

$uptime (Allows user to see the amount of time the system has been running)


# **Command Types in Linux**
* Internal or Built in commands. 
* External Commands
To check if the command is internal or external command we need to use the below command

$ type echo (Prints as internal command)

$ type mv (Prints as External Command)


